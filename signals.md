# 每日信号 — 2026-09-23

数据口径：机械数字全部来自 `scripts/signal_facts.py`，不自行重算。

> ⚠️ **数据过期**：四只标的 `as_of` 都是 **2026-09-18（上周五）**，缺 09-21/09-22/09-23
> 三个交易日。原因：今天 `logs/fetch.log` 里抓取脚本连 OpenD 一直报
> `Disconnected ... reason=RemoteClose` / `init connect fail`，而 `127.0.0.1:11111`
> 端口是在监听的——更像是 OpenD 登录会话失效（需要 Nick 交互重新登录一次），
> 不是进程没起。下面所有结论都基于 09-18 收盘数据，**Nick 修好 OpenD 后建议重跑一次**。

`positions.json` = `{}` → 四只全部按**空仓**处理（跟 09-22 核对过的 moomoo 账户
零持仓一致，不再怀疑漏记）。

| 代码 | 状态A | 当前开窗 | 绿柱日 / 参考价 / 突破 | 行情段自查 | 结论 |
|---|---|---|---|---|---|
| NVDA | 是 | 2026-09（绿转红，**本月未收完**） | 未到（次月才开始找） | 倾向新起点 | 等9月收盘确认窗口，10月起找绿柱日 |
| AMD | 是 | 2025-08（旧窗） | 2025-09-02 / 186.65 / 2025-10-06 无效 | —（已错过） | 不追，等下一次开窗 |
| STZ | 否 | 无 | — | — | 无信号 |
| QQQ | 是 | 2026-04 | 2026-05-19 / 721.2369 / 2026-05-22 有效 | 半路（规则上行情段仍在跑） | 入场点已是4个月前，现在不是规则内的新入场 |

---

## US.NVDA — 等待（本月开窗待确认）

- 状态A 成立：`latest_month` DIF 29.37 / DEA 29.00 均>0。
- 开窗：2026-09 月柱 +0.7518，前两月 07 月 -1.26（绿）、08 月 +0.17（|柱|<0.5 贴0轴→按绿）
  → 9月是首个可信红柱，绿转红；柱 0.75 ≤ DEA 29.00，满足三条件。
  `open_window_entry_chain` 只有 `window_open_month=2026-09-01`，无绿柱日——正常，
  绿柱日按规则从**开窗次月（10月）第一个交易日**起才开始找。
- **需要注意（我的判断）**：9月K线还没走完（数据只到 09-18），月柱 0.75 离 0.5 贴0轴
  阈值很近。如果到月底柱缩回 <0.5，按规则这个月就算不上开窗，窗口作废。
  所以现在只是"暂定开窗"，**以 09-30 月收盘为准**。
- 现价（09-18 收盘）222.27，无持仓。

**行情段自查 → 倾向"新起点"**（跟昨天的判断不同，理由如下）：
- 03号文档口径看的是"最近一次**按规则入场**后是否已有效出场"，不是"股价这几年
  是不是一直在涨"。`recent_windows` 里 2021/2023/2024/2025-07 那几个老窗口显示
  `closed:false`，只是说明开窗次月柱没变矮，不代表还有行情段在跑。
- `data/backtest_results.md` 里 NVDA 最近一轮完整周期：2025-07-25 入场 →
  2025-11-17 周K有效低点跌破出场；之后 2026-05 窗口次月就关闭（没入场）。
  所以按规则算，目前没有在跑的行情段，也没持仓 → 满足新起点条件。
- 保留意见：回测用的是后复权(hfq)，贴0轴的 0.5 绝对阈值在 hfq 下判出来的窗口
  跟 qfq 不一样（回测表里有一笔 2026-09-04 入场，跟 qfq 下"9月才开窗、10月才找
  绿柱日"对不上）。这不影响今天的结论，但是个口径差异，Nick 有空可以看一下。

**动作**：无。等 9 月收盘确认窗口；确认了，10月开始盯日K第一个柱<0 的交易日（绿柱日）。

## US.AMD — 不追

- 状态A 成立（DIF 99.6 / DEA 65.2）。
- 最近的有效窗口是 2025-08（绿转红），绿柱日 2025-09-02，参考价 186.65；
  2025-10-06 突破当天**开盘价已超参考价5%，判定不追**（`valid=false`）。
  之后一路涨到现价 559.82（09-18），跟参考价已经完全不在一个位置了。
- 9月月柱 68.96 比 8月 67.13 略长（像矮转高），但柱 68.96 > DEA 65.17，
  按第三条"柱>DEA=高位追高"，不开窗——脚本也没列这个窗口，一致。
- 行情段：这波规则上没入场（错过了），也没持仓，不存在"在跑的行情段"，
  但当前没有新窗口，谈不上新起点。

**动作**：无。规则明确"超5%不追，等下一次"——等月柱回落后下一次合规开窗。

## US.STZ — 无信号

- 状态A **不成立**：DIF -19.33 / DEA -18.84，双线都在0轴下。
- 月柱从 2026-04 起一直在 ±0.8 附近磨（贴0轴），9月转 -0.98；价格 118.97 创近两年新低。
- `open_window_entry_chain=null`。回测里上一轮是 2021-12 入场、2025-01 触发 -20% 止损出场。

**动作**：无。继续观察，双线回到0轴之上前不用看。

## US.QQQ — 有效突破已过去4个月，现在不是规则入场点

- 状态A 成立（DIF 69.40 / DEA 63.33）。
- 当前窗口 2026-04（绿转红），绿柱日 2026-05-19，参考价 721.2369，
  **2026-05-22 突破有效**（`valid=true`）。
- 现价 721.45（09-18）又回到参考价附近。
- **我的判断**：这个"有效突破"是 5 月的事件。规则的入场动作是"绿柱日后**首个**
  盘中high>参考价 → 买入"，那一刻是 05-22，已经过去了。Nick 当时没进场（持仓为空），
  现在价格回到参考价位置，**不等于重新出现了入场信号**。
- 行情段自查：按回测，05-22 那笔入场到数据结束还没出场（"数据结束仍持有"），
  也就是按规则，这段行情还在跑 → 现在进场属于**老行情半路**，不当新机会。
- 附带观察：月柱 6月 20.97 → 7月 15.82 → 8月 14.34 → 9月 12.14 连续缩短，周K柱
  连续为负，动能在减弱。只是描述，不是规则信号。

**动作**：规则上不建议按 5 月的信号补票。如果 Nick 想按自己判断买 QQQ，那是规则外的
决定，没有对应的机械参考价/止损；想走规则就等这段行情出场后的下一次开窗。

---

### 持仓检查

`positions.json` 为空，没有需要检查止损线 / 周K有效低点的持仓。

### 系统问题（需要 Nick 处理）

1. **OpenD 连接失败**（最优先）：今天抓取失败，数据停在 09-18。端口在监听但连接被
   对端关闭，大概率要重新交互登录 OpenD。修好后重跑 fetch + 本信号任务。
2. hfq 回测和 qfq 实时判断的开窗月不一致（NVDA 为例），见上面 NVDA 段。
